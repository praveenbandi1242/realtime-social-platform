spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop

  sql:
    init:
      mode: never

app:
  jwt:
    secret: test-secret-key-that-is-long-enough-for-hs256-testing
    access-token-expiration: 900000
    refresh-token-expiration: 604800000